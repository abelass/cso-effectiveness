#CACHE{24*3600}
<BOUCLE_art(ARTICLES){id_article}{statut IN redac, publie}>[, (#ENV{virgule}|non)]<:par_auteur:> 
	#SET{extra,#EXTRA|table_valeur{OP_pseudo}}
	<BOUCLE_auteurs(AUTEURS){id_article}>	
	[(#ID_AUTEUR|=={469}|non) #NOM]	
	[(#ID_AUTEUR|=={469}|oui)
	[(#GET{extra}|sinon{#NOM})]
	]
	</BOUCLE_auteurs>
</BOUCLE_art>